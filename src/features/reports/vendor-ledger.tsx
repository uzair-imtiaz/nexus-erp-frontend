import React from "react";
import EntityLedger from "./entity-ledger";

const VendorLedger: React.FC = () => {
  return <EntityLedger entityType="vendor" title="Vendor Ledger Report" />;
};

export default VendorLedger;
